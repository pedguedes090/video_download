# Environment Configuration
NODE_ENV=production

# Server Configuration
PORT=3000
HOST=0.0.0.0

# For custom base URL (optional - if not set, will auto-detect from request headers)
# BASE_URL=https://your-domain.com

# Production deployment examples:

# Heroku - no configuration needed, auto-detects
# Vercel - no configuration needed, auto-detects  
# Railway - no configuration needed, auto-detects
# Render - no configuration needed, auto-detects

# For custom domain deployment:
# BASE_URL=https://your-custom-domain.com
